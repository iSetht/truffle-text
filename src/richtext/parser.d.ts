export {
  parseRichText,
  normalizeDocument,
  plainTextOf,
  type RichDocument,
  type RichTextContent,
  type StyledRun,
} from '../index.js';

export function isRichTextDocument(value: unknown): boolean;
